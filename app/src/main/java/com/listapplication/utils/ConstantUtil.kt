package com.listapplication.utils

/**
 * Helper class to hold all the application constant.
 */
class ConstantUtil {
    companion object {
        const val DEFAULT_REQUEST_TIMEOUT: Long = 30

        //DB Constant
        const val DB_VERSION = 1
        const val DB_NAME = "repo_db"
        const val TABLE_REPO = "table_repo"
    }
}